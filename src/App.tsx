import React, { FC } from 'react';
import { Button } from 'antd';
import './App.less';
import './mock/interface/book/book_api';
const App: FC = () => (
  <div className="App">
    <Button type="primary">Button</Button>
  </div>
);

export default App;